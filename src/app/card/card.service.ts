import {Card} from './card.model';
import {Injectable} from '@angular/core';
@Injectable()
export class CardService {
  constructor() {}
  moveHorizontal(card: Card) {}
  moveVertical(card: Card) {}
}
